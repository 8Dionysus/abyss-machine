# systemd Units

This district contains installable system and user unit skeletons used by the
bootstrap flow.

Bootstrap may render these into `/etc/systemd/system` and the operator's user
unit directory when explicitly applied. Enabling units remains opt-in.
