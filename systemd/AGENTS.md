# systemd Route

`systemd/` owns Linux/systemd unit skeletons for the host-machine layer.

Units here are source-managed templates. They are not proof that a service is
installed, enabled, or healthy on any real host.
