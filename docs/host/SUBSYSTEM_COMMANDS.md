# Subsystem Command Ownership

`abyss-machine` still exposes one CLI entrypoint, but subsystem behavior should
not be owned by argument parsing code. The command boundary is:

- `src/abyss_machine/cli.py`: argparse wiring, command dispatch, host adapter
  calls, latest/history writes, and human/JSON output.
- package modules under `src/abyss_machine`: stable contracts, policy rules,
  read-model shapes, and pure selection/validation logic.
- `scripts/validators`: public-safe projection and portability checks, not
  hidden product behavior.

## Current Map

| Surface | Current owner | Category | Notes |
|---|---|---|---|
| root/install paths | `abyss_machine.path_policy` | module-owned | Shared by bootstrap and CLI. |
| shared validation summary, validation-document envelopes, and generic subsystem validate/all envelopes | `abyss_machine.validation_contracts` | module-owned | CLI supplies current time, checks, paths, latest refs, and subsystem-specific facts while preserving existing validation JSON shape. |
| typing/nervous roots, services, and typing paths/index documents | `abyss_machine.typing_nervous_policy` | module-owned | Keeps opt-in organs public without live evidence; CLI supplies current path snapshot and today path, then writes latest/index. |
| typing nervous refresh decisions | `abyss_machine.typing_nervous_refresh` | module-owned | Includes refresh field naming, nervous-processing readiness document, and nervous-processing acceptance. CLI adapts refresh orchestration, live facts/index reads, and latest writes. |
| typing/nervous latest-history persistence, JSONL history reads, latest read envelopes, and Codex session-tail filesystem reads | `abyss_machine.typing_nervous_adapters` | first live-adapter seam | Owns atomic JSON latest writes, daily JSONL history appends, local JSONL file discovery, bounded recent-record reads, source-adapter scan accounting, optional typing index writes, latest read-schema envelopes, Codex session-tail file discovery, root-contained state-file selection, incremental byte reads, fallback tail reads, and stat failure reports. CLI still supplies documents, configured paths, current time, ingest execution, state/latest writes, and command rendering. |
| Codex prompt/session-tail semantic ingest boundary | `abyss_machine.typing_codex_semantics` | module-owned semantic adapter | Owns UserPromptSubmit text extraction, prompt-hook metadata/context ingest plans, session JSONL user-message route recognition, IDE/goal/environment envelope normalization, near-line duplicate semantics, Codex session-tail metadata/context ingest plans, and public-safe event summaries. CLI owns JSON parsing, configured paths, session-tail state bookkeeping, `typing_ingest` execution, latest/history writes, and command rendering. |
| browser/WebExtension native-host typing ingest, native-host transport, Firefox profile discovery, and temporary-profile selftest runtime boundary | `abyss_machine.typing_browser_adapters` | module-owned semantic/runtime adapter | Owns browser-extension committed-text ingest plans, browser AI transcript cleanup/metadata ingest plans using `typing_capture_contracts` as policy owner, public-safe post-ingest status documents, synthetic browser-extension and AI transcript selftest message/document builders, native-host route/response/error envelopes, framed native-host little-endian length-prefix read/write, JSON decode/encode, malformed-frame errors, Firefox `profiles.ini` parsing, release-profile selection, safe Firefox selftest profile prefs, WebExtension command selection, browser-context public-safe page serving, browser AT-SPI, focused-browser, and browser-privacy synthetic safe page serving, temp profile/artifact/cache roots, subprocess lifecycle/cleanup, capture env override restoration, probe polling, targeted AT-SPI callback routing, and public-safe WebExtension/browser-context/browser AT-SPI/focused-browser/browser-privacy selftest result assembly. CLI owns binding native-host transport to stdin/stdout, configured profile path/home, `typing_ingest` execution, latest/history writes, browser selftest store/index writes, callback binding, policy reads, and command rendering. |
| AT-SPI focused snapshot, text-event listener/sample, debounce, object-runtime, traversal, path-targeted focus/text/insert, URL-targeted focused-text, URL-scanned text insertion, GI/Atspi frame focus, browser/privacy selftest record readers, and generic GUI selftest boundary | `abyss_machine.typing_atspi_adapters` | module-owned semantic/runtime adapter | Owns focused-snapshot ingest plans/documents, AT-SPI text-event sample envelopes, bounded browser-context summaries, metadata shaping, context identity/debounce decisions, safe string handling, supplied-object state flags, text payload reads, object path/document/application context projection, text-event listener runtime (`pyatspi` loading, Registry register/start/stop, timers, heartbeat, counters, compact-history callback routing, failure documents), focused-candidate tree walk, path/URL browser focus metadata traversal, path-targeted text focus/read/insert runtime, URL-targeted focused-text runtime, URL-scanned GI/Atspi text insertion runtime, GI/Atspi Firefox frame focus runtime, browser/privacy selftest recent-record lookup and absence-proof summaries, accessibility focus actions, typing-event summaries, and generic GUI selftest plans/documents. CLI owns policy reads, capture-gate decisions, browser-context inference callbacks, `typing_ingest` execution, latest/history writes, browser AT-SPI/focused-browser callback binding, and command rendering. |
| saved-text scan filesystem boundary | `abyss_machine.typing_saved_text_adapters` | module-owned filesystem adapter | Owns bounded scan limits, expanded scan roots, path allow/deny/low-signal filtering, root-missing/seen-file accounting, stat/read/decode rejection, state-continuity comparison, ingest kwargs, event summaries, state entries, disabled documents, and public-safe scan documents. CLI owns configured policy reads, `typing_ingest` execution, saved-text state/latest/index writes, timer/service reads, and command rendering. |
| typing saved-text/browser capture policy, saved-text scan policy status, saved-text recent-record validation, recent record policy/causal shape status, causal project binding/resolution, causal interaction identity/context-anchor and URL/AI recipient helpers, typing process project/dedupe/interaction/continuity/lane/context/recipient helpers, causal-context readmodel assembly, process readmodel assembly, and causal-awareness event/readmodel status, capture-gate decisions, recent browser-content context inference, focused-browser selftest and browser-privacy record summaries, Codex prompt hook/session-tail evidence summaries, Codex prompt route coverage assessment, browser selftest proof summary, browser context fallback status, browser WebExtension, AT-SPI, generic GUI, focused-browser, and browser-privacy selftest validation status, browser AI transcript selftest status and validation status, browser input recency classification/readmodel assembly, status, and validation freshness, AT-SPI text-event policy merge and heartbeat status, AT-SPI compact-history record and contract documents, typing coverage document assembly plus route-note/gap and status decisions, typing status, validate, and end-to-end proof document assembly, Codex session-tail latest-status contracts, and metadata contracts | `abyss_machine.typing_capture_contracts` | module-owned | CLI owns policy file reads, browser/AT-SPI/native-host callback binding, user-systemd status reads, configured roots/paths, and latest/history writes; shared latest/history persistence plus Codex session-tail filesystem reads are routed through `typing_nervous_adapters`, Codex semantic ingest planning is routed through `typing_codex_semantics`, browser native-host ingest planning/response envelopes and Firefox selftest runtimes are routed through `typing_browser_adapters`, AT-SPI focused/text-event/generic GUI semantic/runtime helpers are routed through `typing_atspi_adapters`, and saved-text scan filesystem mechanics are routed through `typing_saved_text_adapters`. |
| nervous index JSONL source discovery/loading/hash rules, source-record parsing/metadata, source policy, build projection, build result/meta envelopes, status/freshness envelopes, SQLite/FTS store/search, vacuum result envelopes, bounded scan, and bounded/full validation document envelopes | `abyss_machine.nervous_index` plus `abyss_machine.nervous_index_adapters` | module-owned contracts plus lifecycle adapter | `nervous_index` owns JSONL discovery/parsing, source policy, redacted projection, SQLite/FTS schema/search/store contracts, status/freshness documents, validation envelopes, and vacuum result envelopes. `nervous_index_adapters` owns source-index DB connection binding, schema file writes, file locks/active-lock probes, latest JSON writes, generated DB file mode/group normalization, and vacuum execution through fakeable ports. CLI owns config/privacy/source binding, redactor invocation, derived-record refresh orchestration, local latest/history counts, timer probes, validation fact collection, host policy gates, and rendering. |
| typing/nervous secret-pattern and high-entropy redaction | `abyss_machine.nervous_redaction` | module-owned | CLI owns file dry-run reads and command-specific schema wrapping. |
| typing/nervous source metadata, redacted text/URL payloads, browser-content record/quality/dedupe/web-context contracts, source state/effective/catalog/lookup, and source-set contracts | `abyss_machine.nervous_sources` | module-owned | CLI supplies current time, schema/version, host uid/gid, live browser adapters, JSONL reads/writes, and latest writes; module owns source envelope, private-payload omission shape, browser-content shaping, source override merge, catalog, and set-result shapes. |
| nervous derived event/episode records, event classification, episode grouping, build envelopes, and validation contracts | `abyss_machine.nervous_events` | module-owned | CLI owns local JSONL root reads, derived-record writes, privacy/source policy reads, latest writes, and command rendering. |
| nervous synthesis selection, candidate build orchestration, path/write-result envelopes, markdown, validation, eval run execution-plan, and deterministic eval run/validate envelopes | `abyss_machine.nervous_synthesis` | module-owned | CLI runs local recall/synthesis/eval dependency commands, reads local episode/event evidence, writes latest/period JSONL/markdown and validation output, and renders command output. |
| nervous semantic schema/freshness/maintenance/store/search and embedding subprocess contracts | `abyss_machine.nervous_semantic` plus `abyss_machine.nervous_semantic_adapters` | module-owned contracts plus sidecar/runtime adapter | `nervous_semantic` owns schema/status/maintenance/store/search contracts and embedding subprocess payload/script/result parsing. `nervous_semantic_adapters` owns semantic DB connection/init/counts, file locks/active-lock probes, source-chunk loading from the lexical index, latest/maintain latest-history writes, sidecar build-run metadata/provenance transactions, generated DB mode/group normalization, embedding temp-file staging, subprocess runner invocation, output readback, cleanup, and resource-profile callback routing. CLI owns semantic config reads, model/python path discovery, policy gates, resource-launch orchestration, build-window orchestration, and rendering. |
| nervous retention route specs, file candidate classification, policy, plan/apply/validate envelopes, and privacy-review route contracts | `abyss_machine.nervous_retention` | module-owned | CLI owns root scanning, symlink-tail checks, unlink execution, latest/history writes, and command rendering. |
| nervous screenshot recurring-extension query policy and capture backend plan | `abyss_machine.nervous_screenshot` | module-owned | CLI owns GNOME extension status probes, process/X11 risk probes, file writes, capture execution, artifact hashing, fact writes, and command rendering. |
| nervous rerank profile/defaults, source-prior scoring, machine-query caps, merge policy, hybrid result scoring, neural text/config shaping, guarded neural-score blending, eval document/check envelopes, hybrid live search orchestration, and rerank/eval latest-history routing | `abyss_machine.nervous_rerank`, `abyss_machine.nervous_rerank_adapters`, and `abyss_machine.nervous_retrieval_adapters` | module-owned contracts plus runtime/search adapters | `nervous_rerank` owns hybrid scoring, neural text/config shaping, guarded neural-score blending, and eval envelopes. `nervous_rerank_adapters` owns OpenVINO scorer temp-payload staging, subprocess runner invocation, stdout/output JSON parsing, policy-gate callback routing, debug input/output path reporting, and resource-profile callback routing. `nervous_retrieval_adapters` owns lexical/semantic source collection through fakeable ports, semantic maintenance assessment routing, hybrid rerank document assembly, and latest/history write routing for rerank search/eval. CLI owns concrete config/path/callback binding and command rendering. |
| nervous recall refusal, mode normalization, search execution-plan, evidence projection, summary counts, pack identity, retrieval-pack document contracts, live search dispatch, and retrieval latest-history routing | `abyss_machine.nervous_recall` plus `abyss_machine.nervous_retrieval_adapters` | module-owned contracts plus search/write adapter | `nervous_recall` owns refusal/mode/search-plan/evidence/pack contracts. `nervous_retrieval_adapters` owns lexical/hybrid search dispatch through fakeable ports, pack document assembly through the recall contract module, and retrieval latest/history writes. CLI owns privacy refusal binding, concrete ports/paths, and command rendering. |
| nervous brief scope/limit/cache keys, semantic-maintenance thresholds, recent-episode compact projection, readiness/gap/next-action decisions, and brief document envelope | `abyss_machine.nervous_brief` | module-owned | CLI owns live quality/status/privacy/capture/index/semantic/synthesis/host read-model collection, episode JSONL reads, latest/history writes, and command rendering. |
| nervous quality derived-refresh status, validation compaction, check matrix, and audit document envelope | `abyss_machine.nervous_quality` | module-owned | CLI owns refresh execution, live validators/systemd/latest/redaction input collection, latest/history writes, and command rendering. |
| nervous privacy defaults, state merge/normalization, effective privacy, status, set-transition, audit-record, and set-result contracts | `abyss_machine.nervous_privacy` | module-owned | CLI owns config/state file reads, state writes, audit JSONL appends, latest writes, and command rendering. |
| resource policy, gates, systemd plan, and launch argv | `abyss_machine.resource_planning` | module-owned | CLI gathers live mode/memory/storage/game/thermal/CPU inputs and executes `systemd-run`. |
| AI CPU route selection and routed-heavy policy | `abyss_machine.ai_cpu_routing` | module-owned | CLI gathers live thermal/policy/mode/battery facts and writes latest route evidence. |
| AI runtime env/cache/resource-profile/model inventory, LLM paths/registry/validate/runtime/profile status, OpenVINO benchmark-plan/probe/eval command/result contracts, AI eval suite-policy/execution-plan/STT scoring/result envelopes, token-accounting privacy/count/count-execution/profile/tokenizer-route/aoa-summary contracts, AI capabilities projection, AI policy decision/gate, workload taxonomy/measurement extraction plus stats/refresh/status routing, AI paths/status/runtime/report read-model envelopes, runtime/model/package/tokenizer discovery adapters, bounded OpenVINO benchmark/eval runner adapters, AI env/resource binding adapters, AI capabilities input-collection adapter, AI policy input-collection adapter, STT eval dictation-transport adapter, STT synthetic fixture adapter, benchmark/eval suite orchestration adapters, workload JSONL/readmodel store adapters, core AI readmodel store adapters, AI policy-gate binding adapter, LLM registry/latest/validate store and input-collection adapters, token-accounting tokenizer runner/store/readmodel adapters, `.aoa` generated-summary adapter, resident LLM controller runner/result-projection adapter, and workhorse LLM controller runner/result-projection adapter | `abyss_machine.ai_runtime_contracts` plus `abyss_machine.ai_runtime_adapters` | module-owned contracts plus runtime adapter | `ai_runtime_contracts` owns stable document/policy shapes. `ai_runtime_adapters` owns configured model-root normalization, OpenVINO runtime probes, RPM/ldconfig/NPU driver discovery, model inventory filesystem walks, `llama.cpp` runtime/profile file probes, tokenizer/library discovery, OpenVINO python package-version probes, kernel-module snapshots, AI subprocess env binding through fakeable environment/root ports, resource snapshot/profile assembly through fakeable memory/thermal/battery/rusage/load ports, capabilities input collection through fakeable devices/models/dictation/TTS/LLM registry/resident-latest ports, policy readmodel input collection through fakeable observability/mode/battery/thermal/CPU ports, OpenVINO smoke/embedding/text child-process runner execution, STT eval dictation-client transport timing/resource envelopes, STT synthetic fixture directory/path setup, `espeak-ng`/`ffmpeg` execution, raw WAV cleanup, and WAV metadata reads, benchmark/eval device-plan and suite execution orchestration, whole-command resource before/after sampling, benchmark/eval latest and daily JSONL write routing, workload-measurement callback routing, workload run JSONL discovery/tolerant reads/dedupe appends, workload refresh-from-latest source gating, workload taxonomy/stats/refresh/status write routing, devices/models/capabilities/policy/runtime/status/report readmodel assembly plus latest/history write routing, policy-gate callback/clock/class-level binding through fakeable ports, LLM registry assembly, registry latest/history routing, latest readmodel reads, and LLM validate document/input-collection/write routing through fakeable ports, token-accounting tokenizer subprocess command/env/timeout/timing execution, token-accounting contract/profiles/latest/count readmodel and store routing, `.aoa` generated-summary session-registry/manifest/index reads plus latest/history write routing, resident LLM controller command/timeout/subprocess execution plus JSON/result projection, and workhorse LLM controller command/timeout/subprocess execution plus JSON/result projection. CLI owns concrete config/path binding, concrete live reader/env source selection, and command rendering. |
| AI TTS profile/artifact/status, denial/error, server response/payload, synth subprocess script/argv/result, synth/eval/compare result, success-index helper contracts, server socket transport, server status/stop probe exchange, warm server socket/request loop, OpenVINO import/load/generate/write lifecycle, shutdown cleanup, synth subprocess env binding, cold synth child-process runner execution, output audio summary, and synth runtime resource-report assembly | `abyss_machine.ai_tts_contracts` plus `abyss_machine.ai_tts_adapters` | module-owned contracts plus TTS execution adapter | `ai_tts_contracts` owns stable TTS document and command/result shapes. `ai_tts_adapters` owns Unix-socket JSON-line client transport, server status/stop request exchanges, warm server socket bind/request loop/JSON command dispatch, Qwen3 OpenVINO SDK import/path injection, model-load config construction, model load/unload callback, request-to-SDK mapping, generate call, `soundfile` WAV write, TTS subprocess cache/env construction, BabelVox/Qwen3 OpenVINO synth subprocess execution, output WAV summary, wall-clock runtime report, RTF derivation, and resource-profile callback routing through fakeable ports. CLI owns profile/config selection, policy gates, latest writes, and command rendering. |
| doctor policy/path/status/report, validate document, machine-report document contracts, validate probe collection, core status probe collection, power/cooling status probe collection, storage/process status probe collection, snapshot/observability status probe collection, dictation status probe collection, report IO, machine-report input collection, and safe repair orchestration | `abyss_machine.doctor_contracts` plus `abyss_machine.doctor_adapters` | module-owned contracts plus diagnostic adapter | `doctor_contracts` owns stable document shapes. `doctor_adapters` owns `doctor validate` file/latest/systemd/bridge probe check collection, `doctor` platform/path/topology/stack-bridge/binary/command availability checks, `doctor` power/cooling status checks through fakeable status/systemd ports, `doctor` storage/process status checks through fakeable facts/storage/process ports, `doctor` snapshot/observability status checks through fakeable status-readmodel ports, `doctor` dictation status checks through fakeable status/service/path ports, report daily-path/write mechanics, compact machine-report artifact reads, machine-report input collection through fakeable doctor/memory/nervous/AI/artifact ports, safe repair gating/execution selection/result compaction, and machine-report latest/history/markdown writes. CLI still owns deeper doctor status live probes, concrete live port binding, latest/history writes, and command rendering. |
| memory policy/path, pressure-classification, zram-relief, headroom attribution, launch-gate, and plan document contracts | `abyss_machine.memory_contracts` | module-owned | CLI owns `/proc`, `/sys`, cgroup/systemd reads, process sampling, orchestration/apply routes, latest/history writes, and command rendering. |
| mode policy/path/state, definitions, target-profile, thermal launch caps, external profile-guard decisions, and plan/status/validate/reconcile-light document contracts | `abyss_machine.mode_contracts` | module-owned | CLI owns live battery/power-profile/sensor/cooling/AI CPU/storage/memory/process sampling, `set`/`reconcile` host mutation, cooling apply, systemd/file reads, latest/history writes, and command rendering. |
| observability path/latest/status/manual-probe and sample-temperature contracts | `abyss_machine.observability_contracts` | module-owned | CLI owns collector subprocess execution, filesystem permission probing, line counts, systemd reads, live latest reads, and command rendering. |
| cooling config/path/status/recommend/apply envelope, RAPL smoothing state/status, fan-level parsing, and fan-series decision contracts | `abyss_machine.cooling_contracts` | module-owned | CLI owns `/sys`, `systemd`, `journalctl`, platform/fan/RAPL writes, live samples, latest/history writes, and command rendering. |
| process role/workload/game classifiers, paths/latest read models, game-guard envelope, snapshot summary/top-list contracts, process `/proc` snapshot adapter, process container-health adapter, and process desktop-compositor command/proc adapter | `abyss_machine.process_contracts` plus `abyss_machine.process_adapters` | module-owned contracts plus process live adapter | `process_contracts` owns role/workload/game classifiers and document/read-model shapes. `process_adapters` owns low-level `/proc` stat/status/cmdline/io/cgroup/fd reads, storage-root matching, process info assembly, interval CPU jiffy sampling through fakeable proc-root/sysconf/sleep ports, sanitized Podman `ps`/`inspect` container-health projection through fakeable command ports, and read-only GNOME Shell desktop-compositor command/proc probes through fakeable command/proc ports. CLI owns protected game-root/storage-root binding, gamemode status, system summaries, AT-SPI hard-timeout desktop capture, thermal sampling, broader container orchestration, latest/history writes, and command rendering. Storage hook execution is owned by `storage_adapters`. |
| heartbeat/reaction/response paths, heartbeat source freshness/rhythm/candidate lifecycle, reaction candidate/status envelopes, owner-gated response command profile/route/status contracts, and validate documents | `abyss_machine.runtime_evidence_contracts` | module-owned | CLI owns live latest reads, systemd/PSI probes, self-awareness refreshes, reaction source collection, response route-depth validation, latest/history writes, and command rendering. |
| storage policy env/read model, hook stages/status envelope, cache env routes, inventory drift, pressure classes/recommendations, cleanup action contracts, protected-root decisions, write-preflight decision, dry-run apply shape, storage paths read model, cleanup-plan active-process guard adapter, allowlisted cleanup apply execution adapter, storage hook execution adapter, and storage inventory measurement adapter | `abyss_machine.storage_contracts` plus `abyss_machine.storage_adapters` | module-owned contracts plus storage live adapter | `storage_contracts` owns policy/read-model/action decisions, hook stage/status contracts, inventory drift, and pressure rules. `storage_adapters` owns cleanup-plan path matching, process snapshot projection, `/proc` fd target inspection through fakeable ports, active-process guard summaries, non-claim text, allowlisted cleanup apply execution through fakeable command-runner/euid/clock ports, storage hook directory scan/execution through fakeable hook-runner/env ports, and inventory path/disk measurement through fakeable `du` runner, disk-usage, clock, and path-scan ports. CLI owns policy file reads, configured hook directory/env/time binding, inventory spec selection, podman/memory input binding, process snapshot binding, monitor/status orchestration, apply preflight orchestration, latest/history writes, and command rendering. |
| changes paths/index/status/latest read models, change id and decision-review contracts, record/event/result shapes, surface classification, and preflight decision envelope | `abyss_machine.changes_contracts` | module-owned | CLI owns active/closed directory scans, JSON reads, decision-ref existence checks, record/close file writes, directory moves, latest/history writes, and command rendering. |
| docs markdown/decision-record parsing, decision-index document, docs paths document, docs index document, docs agents-mesh validate document, and docs spec-id contracts | `abyss_machine.docs_contracts` | module-owned | CLI owns source document discovery, file reads/stat/hash input collection, agent-mesh discovery/check collection, audit orchestration, generated index writes, latest/history writes, and command rendering. |
| topology paths/status/index, validate document envelope, surface-state vocabulary, and forbidden-root classifier contracts | `abyss_machine.topology_contracts` | module-owned | CLI owns live path stats, topology validate/audit checks, bridge/ledger validation, installed-binary checks, latest/history writes, and command rendering. |
| host lifecycle source/install/runtime parity summary | `abyss_machine.host_lifecycle_parity` plus `scripts/validators/source_install_runtime_parity.py` | module-owned compact contract plus validator adapter | `host_lifecycle_parity` owns the compact parity schema, path identity summaries, digest-map comparison, mismatch counts/samples, runtime JSON projection, runtime closeout command catalog/profile selection, runtime effect labels, fake-runner collection, and no-raw-runtime-output privacy rule. The validator script owns host path defaults and bounded subprocess invocation for source/install/runtime closeout; commands that refresh latest/readmodel state require explicit opt-in. |
| graph node/query/index/validate document, maps policy/path/entry/query/packet/validate document, and machine RAG trace/eval/validate/status/index document contracts | `abyss_machine.context_contracts` | module-owned | CLI owns bridge/topology/subsystem collection, graph/maps validate checks, source-ref stat/hash reads, generated atlas writes, bounded evidence snapshot reads, systemd/timer validation, refresh orchestration, latest/history writes, and command rendering. |
| stack-bridge paths, extension commands, typing/heartbeat/observability handoff contracts, heartbeat readiness, export envelope, latest-read contract, and validate document | `abyss_machine.stack_bridge_contracts` | module-owned | CLI owns artifact route catalog assembly, latest JSON reads, static sync planning/writes, observability HTTP probes, validation orchestration, latest/history writes, and command rendering. |
| self-awareness paths, bridge command contracts, validate document, read-only event/fabric contracts, redaction/bounded-shape helpers, query matching, time buckets, stack-handoff service mapping, working-stack role/status/link identity, and activation-gap handoff route contracts | `abyss_machine.self_awareness_contracts` | module-owned | CLI owns live stack/runtime probes, latest JSON reads, refresh orchestration, investigate/replay/probe/cycle execution, validation reads/writes, and command rendering. |
| dictation config/profile/runtime env, runtime paths/sockets, paths/index docs, replacements, postprocess, intent, busy/max-duration, audio-doctor summary/recommended-runtime decisions, mic-calibration command/result contracts, recording command/state, stop/toggle lifecycle envelopes, transcript helper/server request/result envelopes, insertion result/key-sequence policy, journal event/markdown contracts, explicit-file transcription runtime execution, recording lifecycle/process-state execution, read-only audio inspection/probe execution, transcript journal read/write IO, clipboard/text insertion execution, mic-calibration recording/apply, lock execution, notification execution, status read-model/readiness probes, validation/latest routing, and replacements read/write flow | `abyss_machine.dictation_contracts`, `abyss_machine.dictation_runtime_adapters`, `abyss_machine.dictation_profile_adapters`, `abyss_machine.dictation_docs_adapters`, `abyss_machine.dictation_execution_adapters`, `abyss_machine.dictation_lock_adapters`, `abyss_machine.dictation_postprocess_adapters`, `abyss_machine.dictation_notifications_adapters`, `abyss_machine.dictation_status_adapters`, `abyss_machine.dictation_validation_adapters`, and `abyss_machine.dictation_replacements_adapters` | module-owned contracts plus runtime/profile/docs/execution/lock/postprocess/notification/status/validation/replacements adapters | `dictation_contracts` owns stable shapes. `dictation_runtime_adapters` owns XDG runtime root fallback, runtime dir creation, server socket, ydotool socket, and max-duration env translation through fakeable/public-safe ports. `dictation_profile_adapters` owns config load/save, concrete profile defaults, env-bound runtime/postprocess/profile selection, runtime env projection, and config/profile read documents. `dictation_docs_adapters` owns daily transcript path projection, paths/index/AGENTS.md documents, directory creation, AGENTS.md touch/update, and index JSON writes. `dictation_execution_adapters` owns warm-server socket transport, client-side 16 kHz preprocessing, helper subprocess invocation, transcript result mapping, recording state-file IO, active/stale reads, process start/liveness/stop signalling, toggle debounce, WAV stats, recent WAV discovery, audio-doctor probes, journal policy and read/write IO, `wtype`/`wl-copy`/`ydotool` insertion, and mic-calibration flow. `dictation_lock_adapters` owns file-lock execution. `dictation_postprocess_adapters` owns transcript fixes, intent detection, and postprocess application. `dictation_notifications_adapters` owns notification policy and `notify-send` spawning. `dictation_status_adapters`, `dictation_validation_adapters`, and `dictation_replacements_adapters` own their established read-model, validation, and replacement mutation seams. CLI owns concrete env/config binding, callback wiring, dispatch, and command rendering. |
| artifact bundle/trust read models and `artifacts validate` document envelope | `abyss_machine.artifact_bundles` | other lane with module-owned validate envelope | Artifact/release trust is not the main host command-glue lane; CLI still owns collected checks, latest/index writes, and rendering. |
| CLI help, exit codes, text rendering | `src/abyss_machine/cli.py` | CLI edge | Keep this thin and parity-tested across source and installed projections. |
| nervous event/episode local latest/write adapters, nervous retention filesystem/unlink/latest adapters, screenshot live probe/capture adapters, AI runtime broader resource-sampling/resident readmodel adapters, and self-awareness live probe/readmodel orchestration | `src/abyss_machine/cli.py` | remaining adapter debt | Split only when the reusable center is clear and tests can constrain it. |

## Full CLI Surface Audit

The current parser exposes more than the module-owned v1 surfaces above. This
audit is the command-glue blind-spot guard: it names the broad command groups
that still exist in `src/abyss_machine/cli.py` so a narrow extraction is not
mistaken for whole-CLI completion.

| Command surface | Current classification | Owner / next action |
|---|---|---|
| `facts`, `status`, `bridge`, `enter`, `modes`, `snapshots` | CLI edge / generated read-model access | Keep thin unless stable read-model contracts grow large enough for a module. |
| `doctor` | partial module ownership / remaining debt | `doctor_contracts` owns stable policy/path/status/report, validate document, and machine-report contracts. `doctor_adapters` owns `doctor validate` probe check collection, `doctor` core platform/path/topology/stack-bridge/binary/command availability checks, `doctor` power/cooling status checks, `doctor` storage/process status checks, `doctor` snapshot/observability status checks, `doctor` dictation status checks, report/machine-report IO for latest/history/markdown and compact artifact reads, machine-report input collection through fakeable live ports, and safe repair orchestration through fakeable repair runners. Deeper status probes, concrete live binding, latest/history writes, and rendering remain CLI edge. |
| `topology` | partial module ownership / remaining edge debt | `topology_contracts` owns paths/status/index, validate document envelope, surface-state vocabulary, and forbidden-root classifier contracts; CLI owns live path stats, validate/audit checks, bridge/ledger validation, installed-binary checks, latest/history writes, and rendering. |
| `graph`, `maps`, `rag` | partial module ownership / remaining edge debt | `context_contracts` owns graph node/query/index/validate document, maps policy/path/entry/query/packet/validate document, and RAG trace/eval/validate/status/index document contracts; CLI owns source collection, graph/maps validate checks, source-ref stat/hash reads, generated atlas writes, bounded evidence snapshot reads, systemd/timer validation, refresh orchestration, latest/history writes, and rendering. |
| `stack-bridge` | partial module ownership / remaining edge debt | `stack_bridge_contracts` owns paths, extension command map, typing/heartbeat/observability handoff contracts, heartbeat readiness, export envelope, latest-read contract, and validate document; CLI owns artifact route catalog assembly, latest JSON reads, static sync planning/writes, observability HTTP probes, validation orchestration, latest/history writes, and rendering. |
| `self-awareness` | partial module ownership / remaining edge debt | `self_awareness_contracts` owns paths, bridge command contracts, validate document, read-only event/fabric contracts, redaction/bounded-shape helpers, query matching, time buckets, stack-handoff service mapping, working-stack role/status/link identity, and activation-gap handoff route contracts; CLI owns live stack/runtime probes, latest JSON reads, refresh orchestration, investigate/replay/probe/cycle execution, validation reads/writes, and rendering. |
| `docs` | partial module ownership / remaining edge debt | `docs_contracts` owns markdown/decision-record parsing, decision-index document, docs paths document, docs index document, docs agents-mesh validate document, and spec-id contracts; source discovery, file reads/stat/hash input collection, agent-mesh discovery/check collection, audit orchestration, generated index writes, latest/history writes, and rendering remain CLI edge. |
| `changes` | partial module ownership / remaining edge debt | `changes_contracts` owns paths/index/status/latest read models, id and decision-review contracts, record/event/result shapes, surface classification, and preflight decision envelope; active/closed scans, JSON reads, decision-ref existence checks, record/close mutation, directory moves, latest/history writes, and rendering remain CLI edge. |
| `test` and source/install/runtime parity closeout | CLI-only dispatch plus module-owned compact parity | `test` dispatches validation lanes and is not subsystem product logic. `host_lifecycle_parity` now owns compact source/install/runtime parity summaries, runtime closeout profiles, and read-only vs refresh effect labels for release/install closeout while the validator script binds live installed paths and subprocess execution. |
| `storage` | partial module ownership / remaining edge debt | `storage_contracts` owns policy/env read models, hook stage/status contracts, inventory drift, pressure classification/recommendation rules, cleanup action contracts, protected-root decisions, write-preflight decision logic, dry-run apply shape, and paths read model. `storage_adapters` owns cleanup-plan active-process guard matching, `/proc` fd target inspection, allowlisted cleanup apply execution, storage hook directory scan/execution, and storage inventory path/disk measurement through fakeable ports; live policy reads, configured hook directory/env/time binding, inventory spec selection, podman/memory input binding, process snapshot binding, monitor/status orchestration, apply preflight orchestration, latest/history writes, and rendering remain CLI edge. |
| `artifacts` | other lane / bounded validate-envelope ownership | Artifact/release trust is real but not this command-glue slice. `artifact_bundles` owns the `artifacts validate` document envelope; touch deeper trust/provenance/signing behavior only in its own lane. |
| `memory` | partial module ownership / remaining debt | `memory_contracts` owns stable policy/path, pressure classification, zram relief, launch-gate, headroom attribution, and plan document contracts; live sampling, residency/hotpath/orchestration/apply, and latest writes remain CLI edge. |
| `resource` | partial module ownership | `resource_planning` owns policy/gate/systemd argv contracts; live sampling and launch execution remain CLI edge. |
| `heartbeats`, `reactions`, `responses` | partial module ownership / remaining debt | `runtime_evidence_contracts` owns path surfaces, heartbeat source freshness/rhythm/candidate lifecycle, reaction candidate/status envelopes, owner-gated response command profile/route/status contracts, and validate documents; live latest reads, systemd/PSI probes, self-awareness refreshes, reaction source collection, response route-depth validation, and latest/history writes remain CLI edge. |
| `processes` | partial module ownership / remaining debt | `process_contracts` owns role/workload/game classifiers, paths/latest read models, game-guard envelope, and snapshot summary/top-list contracts. `process_adapters` owns low-level `/proc` process info collection, CPU jiffy sampling, sanitized Podman container-health reads, and read-only GNOME Shell desktop-compositor command/proc probes through fakeable ports. Protected-root binding, gamemode status, AT-SPI hard-timeout desktop capture, thermal attribution/plan orchestration, broader container orchestration, latest/history writes, and rendering remain CLI edge. Storage hook execution belongs to the storage adapter boundary. |
| `nervous` | partial module ownership / remaining debt | Index contracts plus lexical SQLite/FTS lifecycle adapter, derived events/episodes, synthesis build contracts, semantic contracts plus sidecar lifecycle/source-chunk/latest/provenance adapter and embedding subprocess execution adapter, sources/source controls, redaction, retention, retention privacy-review routes, screenshot capture planning, rerank core scoring plus neural subprocess execution adapter, retrieval/rerank live search adapter, recall pack/search-plan shaping, brief readiness shaping, broader privacy contracts, and shared latest/history persistence have modules; screenshot live capture/probes, event/episode local write adapters, retention filesystem/unlink/latest adapters, and most live readers still need bounded audits. |
| `ai` | partial module ownership / remaining debt | CPU routing, runtime contracts, runtime discovery adapters, bounded OpenVINO benchmark/eval child-process runners, env/resource binding adapters, capabilities input-collection adapter, policy input-collection adapter, STT eval dictation-transport adapter, STT synthetic fixture adapter, benchmark/eval suite orchestration and write-routing adapters, workload JSONL/readmodel store adapters, core readmodel store adapters, policy-gate binding adapter, LLM registry/latest/validate store and input-collection adapters, token-accounting tokenizer runner/store/readmodel adapters, `.aoa` generated-summary adapter, resident and workhorse LLM controller runner/result-projection adapters, TTS contracts, TTS client/server-loop/warm-runtime/cold-synth execution adapters, and TTS output audio/resource-report adapters have modules; command rendering remains edge debt, while concrete live reader/env source selection stays CLI-owned by design. |
| `observability` | partial module ownership / remaining debt | `observability_contracts` owns path/latest/status/manual-probe and sample-temperature contracts; collector execution, permission probes, line counts, systemd reads, and live latest reads remain CLI edge. |
| `cooling` | partial module ownership / remaining debt | `cooling_contracts` owns config/path/status/recommend/apply envelope, RAPL smoothing state/status, fan-level parsing, and fan-series decision contracts; `/sys`, `systemd`, `journalctl`, platform/fan/RAPL writes, live samples, validation orchestration, and latest/history writes remain CLI edge. |
| `mode` | partial module ownership / remaining debt | `mode_contracts` owns stable policy/path/state, definitions, target-profile, thermal launch caps, profile-guard decision, and plan/status/validate/reconcile-light document contracts; live sampling, `set`/`reconcile` mutation, cooling apply, and latest writes remain CLI edge. |
| `typing` | partial module ownership / remaining debt | Policy, refresh, capture, causal project binding/resolution, causal interaction identity/context-anchor and URL/AI recipient helpers, typing process project/dedupe/interaction/continuity/lane/context/recipient helpers, causal-context readmodel assembly, process readmodel assembly, and causal-awareness event classification, recent browser-content context inference, focused-browser selftest/browser-privacy/Codex prompt evidence summaries, Codex prompt route coverage assessment, browser selftest proof summary, browser context fallback status, browser input recency classification/readmodel assembly, status, validation freshness, AT-SPI text-event policy merge and heartbeat status, AT-SPI compact-history record and contract documents, typing coverage document assembly plus route-note/gap and status decisions, typing status, validate, end-to-end proof document assembly, Codex session-tail latest-status contracts, sources, redaction contracts, shared latest/history persistence, Codex session-tail filesystem reads, Codex prompt/session-tail semantic ingest planning, browser/native-host ingest plans plus transport/selftest/response envelopes, temporary Firefox WebExtension, browser-context, browser AT-SPI, focused-browser, and browser-privacy selftest runtime, AT-SPI focused/text-event/generic GUI semantic plans, AT-SPI supplied-object runtime helpers, AT-SPI text-event listener runtime, focused-candidate tree walk, browser focus metadata traversal, path-targeted AT-SPI text focus/read/insert runtime, URL-targeted AT-SPI focused-text runtime, URL-scanned GI/Atspi text insertion runtime, GI/Atspi Firefox frame focus runtime, and saved-text scan filesystem mechanics are module/adapter-owned; broader live policy-read/latest-write glue remains CLI edge debt. |
| `dictation` | partial module ownership / remaining edge debt | `dictation_contracts` owns config/runtime/transcript/insert/journal contracts plus audio-doctor summary/recommended-runtime decisions, mic-calibration command/result contracts, recording command/state, and stop/toggle lifecycle envelopes. Runtime path/env, profile/config, docs, execution, lock, postprocess, notifications, status, validation, and replacements seams are adapter-owned. CLI still binds concrete env/config callbacks, command dispatch, and rendering; any remaining dictation debt should be re-inventoried before another extraction claim. |

## Stop Lines

- Do not move private `/var/lib` or `/srv` evidence into Git.
- Do not make `abyss-machine` own `abyss-stack`.
- Do not remove typing/nervous machinery to simplify publication.
- Do not treat artifact/provenance work as the command-glue refactor.
- Do not turn validators into the source of subsystem behavior.

## Future Update Rule

When a command grows stable reusable logic, move that logic into a package
module first, keep live host adapters at the CLI edge, and add module tests plus
CLI/projection parity tests. Only then update this map and the README status.
