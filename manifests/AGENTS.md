# manifests Route

`manifests/` contains machine-readable source-of-truth indexes for the public
repo scaffold.

Keep these files public-safe. They describe expected source surfaces; they must
not include live host evidence.
