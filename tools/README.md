# Tools

`tools/` contains helper probes, harnesses, and migration utilities that are not
yet stable top-level operator commands.

Stable command surfaces belong in `scripts/`. When a tool becomes part of a
durable host movement, route it through the owning mechanic package.
