# Config Templates

This district contains public-safe source templates for host configuration.
Bootstrap renders them into `/etc/abyss-machine` with target-machine paths and
operator choices.

The files here are not live config. Live secrets and local state are provided by
the operator or generated on the target host.
