# env Route

`env/` contains public-safe environment examples only.

Do not commit real `.env` files, API keys, model provider tokens, vault paths
with private material, or host-specific secret locations.
