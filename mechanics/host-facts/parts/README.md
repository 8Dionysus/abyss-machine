# host-facts Parts

Package-local fact collectors and record builders may land here after extraction.

`owner-census-broker/` is the source card for the bounded, authenticated,
read-only process/descriptor census contract. Its executable seam lives in
`src/abyss_machine/owner_census_broker.py`; it has no installed activation.
