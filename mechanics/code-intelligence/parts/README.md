# Provider build inputs

- `scip-python/`: the private npm build-prefix manifest and complete npm v3
  lock for the SCIP Python realization. These are tool-distribution inputs,
  not a lock for an analyzed Python project's resolver environment.
