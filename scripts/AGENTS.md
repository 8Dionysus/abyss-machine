# scripts Route

`scripts/` contains stable operator entrypoints and validators.

Scripts should be runnable from the source checkout and should prefer dry-run
or check modes for publication-sensitive operations.
